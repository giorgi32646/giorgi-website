
  # Add MeshGradientBackground component

  This is a code bundle for Add MeshGradientBackground component. The original project is available at https://www.figma.com/design/ROOuDyBeJhKEkO0Mx45d0p/Add-MeshGradientBackground-component.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  